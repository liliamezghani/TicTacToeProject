<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style des boutons de la grille de jeu -->
    <style name="GridButtonNeonStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:textSize">60sp</item>
        <item name="android:textStyle">bold</item>
        <!-- La couleur du texte sera définie en JAVA (bleu néon ou rose néon) -->
        <item name="android:textColor">#FFFFFF</item>
        <!-- Le background est transparent sur le fond de la grille GridLayout -->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundTint">@null</item>
        <item name="android:stateListAnimator">@null</item> <!-- Supprime l'effet d'ombre -->
        <item name="android:padding">0dp</item>
    </style>

</resources>